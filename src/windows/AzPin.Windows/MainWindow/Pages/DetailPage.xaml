<Page
    x:Class="AzPin.Windows.MainWindow.Pages.DetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AzPin.Windows.MainWindow.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Padding="16,12,16,4">
            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xED25;" FontSize="20"
                      VerticalAlignment="Center" />
            <TextBlock x:Name="RgNameText"
                       Style="{StaticResource SubtitleTextBlockStyle}"
                       VerticalAlignment="Center" />
        </StackPanel>

        <!-- Tab selector -->
        <SelectorBar x:Name="TabBar"
                     Grid.Row="1"
                     Margin="16,0,16,8"
                     SelectionChanged="OnTabSelectionChanged">
            <SelectorBarItem x:Name="PinnedTab" Text="Pinned" Tag="Pinned" />
            <SelectorBarItem x:Name="BrowseTab" Text="Browse" Tag="Browse" />
        </SelectorBar>

        <!-- Tab content frame -->
        <Frame x:Name="TabFrame" Grid.Row="2" />
    </Grid>
</Page>
