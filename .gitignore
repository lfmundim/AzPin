# Xcode
build/
*.xcarchive
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/
xcuserdata/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
DerivedData/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# CocoaPods (not used, but just in case)
Pods/
Podfile.lock

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
.VolumeIcon.icns

# Distribution artifacts
*.dmg
*.pkg
*.ipa
ExportOptions.plist.local
cert.p12

# VSCode
.vscode/
*.code-workspace

# Xcode Cloud / CI
ci_scripts/

# Instruments
*.trace

# Crashlytics (not used, but common noise)
Crashlytics/
Fabric/

# Secrets (never commit these)
*.p12
*.mobileprovision
.env
.env.local
secrets.xcconfig

# Local dev tasks (not source controlled)
src/macos/tasks/